
import { makeStyles } from '@material-ui/core/styles';



export const AssembleStyle = (makeStyles)({
    root: {
      width: '70%',
 

    
    },
  });
 

 export const menuStyles = makeStyles((theme) => ({
    button: {
      margin: theme.spacing(1),
      marginLeft:550,
      marginTop:60,
      maxWidth: '100%',
      display:'flex'
    },
  }));
  export const Selectionimg = makeStyles((theme) => ({
 
      img: {
        margin: 'auto',
        display: 'block',
        maxWidth: '100%',
        maxHeight: '100%',
        width: 400,
        height: 210,
    
      },
    }));